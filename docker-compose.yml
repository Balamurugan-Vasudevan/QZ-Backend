version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quiz-backend
    ports:
      - "8000:8000"
    environment:
      - MONGO_URI=mongodb+srv://balamuruganvasudevanadmin:Bala%4000789@cluster0.jadv4sy.mongodb.net/quizapp?authSource=admin&retryWrites=true&w=majority&appName=Cluster0
      - DB_NAME=quizapp
      - JWT_SECRET=your_secret_key_here_change_in_production
      - JWT_ALGORITHM=HS256
      - JWT_EXPIRE_MINUTES=10080
      - FRONTEND_URL=http://localhost:5173
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
